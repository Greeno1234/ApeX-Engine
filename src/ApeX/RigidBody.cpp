#include "RigidBody.h"
#include "Core.h"
#include "Entity.h"


namespace apex
{
	void RigidBody::on_tick()
	{
		//entity()->core()->find(colliders);

		if (true) //if box collider colides with other collider
		{

		}
	}
}